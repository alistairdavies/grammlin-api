services:
  - type: web
    name: grammlin-api
    runtime: python
    plan: free
    buildCommand: ./scripts/build.sh
    startCommand: uvicorn api.main:api --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.13.11
    healthCheckPath: /health
